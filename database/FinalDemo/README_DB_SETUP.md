# CES Final Demo Database Setup

This package gives everyone the same MySQL database using Docker.

## Requirement

Install Docker first.

## Start the database

From this folder, run:

```bash
docker compose up -d
```

This starts MySQL with:

- host: `localhost`
- port: `33306`
- database: `ces_db`
- username: `root`
- password: `mysqlpass`

## Complete final-demo schema

The database is automatically initialized from:

```text
db/init/00_reset_schema.sql
db/init/01_movies.sql
db/init/02_users.sql
db/init/03_showtimes_and_reservations.sql
db/init/04_orders_and_payments.sql
```

It creates the `movies`, `users`, `addresses`, `payment_cards`, `favorites`,
`email_confirmation_tokens`, `password_reset_tokens`, `showrooms`, `showtimes`,
`seat_reservations`, `promotions`, `orders`, `tickets`, and
`payment_transactions` tables. The scripts also insert seeded movies, users,
three showrooms, scheduled showtimes, held and sold seats, and paid order
history for the final demo.

Note: Docker only runs files in `db/init` when the MySQL volume is first created.
If you already started the Sprint 1 database, apply new scripts manually or recreate
the Docker volume before testing the new tables.

## Rebuild the database

If your local Docker database already has old tables, rebuild it from scratch:

```bash
docker compose down -v
docker compose up -d
```

This deletes the old MySQL volume and reruns all init scripts in order:

1. `00_reset_schema.sql` drops existing project tables
2. `01_movies.sql` recreates and seeds movies
3. `02_users.sql` recreates and seeds users, addresses, cards, favorites, and tokens
4. `03_showtimes_and_reservations.sql` creates and seeds promotions, showrooms,
   showtimes, and seat reservations
5. `04_orders_and_payments.sql` creates and seeds orders, sold tickets, and
   payment transactions

The `showtimes` table prevents two movies from being scheduled in the same
showroom at the same date and time. For the conflict-validation demo, try to
insert another showtime for showroom `1` on `2026-07-30` at `14:00:00`; MySQL
will reject it through the `uq_showtimes_showroom_date_time` constraint.

Seat labels are generated by the application from each showroom's
`row_count` and `seats_per_row`. Temporary holds are stored in
`seat_reservations`, while permanently sold seats are stored in `tickets`, so
a separate `seats` table is not required.

## Final Demo seed data

### Showrooms

| ID | Name | Rows | Seats per row | Total seats |
| --- | --- | ---: | ---: | ---: |
| 1 | Showroom 1 | 5 | 8 | 40 |
| 2 | Showroom 2 | 6 | 10 | 60 |
| 3 | Showroom 3 | 8 | 12 | 96 |

### Promotion

| Code | Discount | Start date | End date |
| --- | ---: | --- | --- |
| `SUMMER20` | 20% | 2026-07-24 | 2026-08-31 |

### Showtimes

| ID | Movie ID | Movie | Showroom | Date | Time |
| ---: | ---: | --- | --- | --- | --- |
| 1 | 1 | Dune: Part Two | Showroom 1 | 2026-07-30 | 2:00 PM |
| 2 | 1 | Dune: Part Two | Showroom 2 | 2026-07-30 | 5:00 PM |
| 3 | 2 | Inside Out 2 | Showroom 3 | 2026-07-30 | 11:00 AM |
| 4 | 3 | The Dark Knight | Showroom 1 | 2026-07-30 | 7:30 PM |
| 5 | 4 | La La Land | Showroom 2 | 2026-07-31 | 2:15 PM |
| 6 | 5 | A Quiet Place | Showroom 3 | 2026-07-31 | 8:45 PM |
| 7 | 6 | Oppenheimer | Showroom 1 | 2026-08-01 | 4:45 PM |
| 8 | 7 | Barbie | Showroom 2 | 2026-08-01 | 6:30 PM |
| 9 | 8 | Spider-Man: Across the Spider-Verse | Showroom 3 | 2026-08-01 | 1:45 PM |

### Temporarily held seats

| Showtime ID | Seat | Status | Ticket type | Owner |
| ---: | --- | --- | --- | --- |
| 1 | B4 | HELD | SENIOR | Guest session `demo-guest-session` |
| 3 | D6 | HELD | ADULT | Guest session `second-demo-session` |

Permanent sold seats are stored in `tickets`, not in
`seat_reservations`.

### Paid orders and sold tickets

| Confirmation | Customer | Movie/showtime | Tickets | Total | Card |
| --- | --- | --- | --- | ---: | --- |
| `CES-20260728-0001` | `customer@cinema.com` | Dune: Part Two, 2026-07-30 2:00 PM | A1 Adult $12; A2 Child $6 | $19.44 | Visa ending 1111 |
| `CES-20260728-0002` | `customer@cinema.com` | Inside Out 2, 2026-07-30 11:00 AM | C5 Adult $12 | $12.96 | Mastercard ending 4444 |

Both orders have matching `APPROVED` rows in `payment_transactions`. These
records support the order-history, confirmation-page, confirmation-email, and
already-sold-seat demo paths.

### Scheduling-conflict test

The following input intentionally conflicts with showtime ID 1 and should be
rejected because showroom 1 is already scheduled at that date and time:

```sql
INSERT INTO showtimes (movie_id, showroom_id, show_date, show_time)
VALUES (2, 1, '2026-07-30', '14:00:00');
```

Expected result: a duplicate-key error naming
`uq_showtimes_showroom_date_time`. This statement is documentation only and is
not executed by the initialization script.

Demo accounts seeded by `02_users.sql`:

| Email | Password | Role | Status | Purpose |
| --- | --- | --- | --- | --- |
| `admin@cinema.com` | `Password123!` | `ADMIN` | `ACTIVE` | Admin login and admin redirect testing |
| `customer@cinema.com` | `Password123!` | `CUSTOMER` | `ACTIVE` | Customer login, profile with 3 payment cards, and favorites testing |
| `inactive@cinema.com` | `Password123!` | `CUSTOMER` | `INACTIVE` | Inactive account login validation testing |
| `nocard@cinema.com` | `Password123!` | `CUSTOMER` | `ACTIVE` | Checkout path that asks for a new payment card |

The password is stored in the database as a BCrypt hash, not as plaintext.

Use `customer@cinema.com` to demonstrate checkout with saved cards and order
history. Use `nocard@cinema.com` to demonstrate checkout when no saved card is
available.

## Checkout data flow

Before payment, selected seats are stored in `seat_reservations`. On successful
payment, the application should perform the following work in one database
transaction:

1. Verify that the held seats still belong to the current user/session.
2. Insert the `orders` row.
3. Insert one `tickets` row per purchased seat.
4. Insert an `APPROVED` `payment_transactions` row.
5. Delete the corresponding temporary `seat_reservations` rows.

The unique constraint on `tickets(showtime_id, seat_label)` permanently
prevents a sold seat from being sold again. Full card numbers and CVVs are
never stored in the order or payment-transaction tables.

## Spring Boot connection

Use these settings in `src/main/resources/application.properties`:

```properties
spring.datasource.url=jdbc:mysql://localhost:33306/ces_db
spring.datasource.username=root
spring.datasource.password=mysqlpass
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=none
```

## Admin showtime API

The showtime endpoints require a logged-in session whose role is `ADMIN`.

| Method | Endpoint | Purpose |
| --- | --- | --- |
| `GET` | `/api/admin/showtimes/movies` | Movie options for the schedule form |
| `GET` | `/api/admin/showtimes/showrooms` | Showroom options, including layout sizes |
| `GET` | `/api/admin/showtimes` | All scheduled showtimes |
| `POST` | `/api/admin/showtimes` | Schedule a movie |

Example request body:

```json
{
  "movieId": 1,
  "date": "2026-07-28",
  "time": "19:30:00",
  "showroomId": 2
}
```

A successful create returns `201 Created`. A duplicate showroom/date/time
returns `409 Conflict` with:

```json
{
  "error": "SHOWTIME_CONFLICT",
  "message": "Showroom 2 already has a showtime at 2026-07-28 19:30"
}
```
